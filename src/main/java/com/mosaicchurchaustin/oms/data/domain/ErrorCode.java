package com.mosaicchurchaustin.oms.data.domain;

public enum ErrorCode {
    NOT_FOUND,
    ORDER_ALREADY_ASSIGNED,
    USER_ALREADY_EXISTS,
    UNAUTHORIZED_ACTION,
    VALIDATION_FAILED,
    UNKNOWN_ERROR
}

